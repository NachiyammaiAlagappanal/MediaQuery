import { Box } from '@mui/material';
import { React } from 'react';
const MediaType = () =>
	<Box sx={ { display: 'flex',
		alignItems: 'center', flexDirection: 'column', gap: 10 } }
	>Print, screen , All</Box>;

export default MediaType;
