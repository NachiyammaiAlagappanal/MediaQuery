import { Box } from '@mui/material';
import { React } from 'react';
const MediaOperator = () =>
	<Box sx={ { display: 'flex',
		alignItems: 'center', flexDirection: 'column', gap: 10 } }
	>AND</Box>;

export default MediaOperator;
