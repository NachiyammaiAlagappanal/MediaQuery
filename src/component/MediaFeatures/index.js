import { Box } from '@mui/material';
import { React } from 'react';
const MediaFeatures = () =>
	<Box sx={ { display: 'flex',
		alignItems: 'center', flexDirection: 'column', gap: 10 } }
	>Min-width and Max-width</Box>;

export default MediaFeatures;
